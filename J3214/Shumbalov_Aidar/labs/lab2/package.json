{
  "name": "omagad-lab2-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "api": "json-server-auth db.json -r routes.json --host 127.0.0.1 --port 3001",
    "web": "vite --host 127.0.0.1 --port 5174",
    "build": "vite build",
    "dev": "concurrently \"npm run api\" \"npm run web\""
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0",
    "vite": "^7.2.4"
  }
}
