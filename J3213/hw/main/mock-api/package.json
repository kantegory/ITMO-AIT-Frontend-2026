{
  "name": "finance-manager-mock-api",
  "private": true,
  "version": "1.0.0",
  "description": "Mock API for finance manager frontend labs",
  "scripts": {
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
