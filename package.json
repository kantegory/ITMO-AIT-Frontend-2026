{
  "name": "online-courses-lab2",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "server": "json-server-auth --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0"
  }
}