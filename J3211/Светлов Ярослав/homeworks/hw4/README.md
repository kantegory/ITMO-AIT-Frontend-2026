# ДЗ4 — SVG-спрайт

Node.js 22+. `npm ci`, затем `npm start`. Адрес: http://localhost:3004.
Аккаунты: `admin@nexus.ai` / `password`, `vision@nexus.ai` / `cv12345`. `npm test` — тесты API.

Пять собственных иконок находятся в `public/sprite.svg`. [Отчёт в PDF](Отчёт.pdf), [исходник LaTeX](report.tex).
