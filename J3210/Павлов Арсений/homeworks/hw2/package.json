{
  "name": "lab2-fintrack-pro",
  "version": "1.0.0",
  "private": true,
  "description": "ЛР2: mock API + авторизация для сервиса личных финансов",
  "scripts": {
    "api": "json-server db.json -m ./node_modules/json-server-auth -r routes.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0"
  }
}
