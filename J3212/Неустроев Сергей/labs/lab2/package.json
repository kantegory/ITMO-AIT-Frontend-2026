{
  "name": "fintracker-lab2",
  "version": "1.0.0",
  "description": "ЛР2 — мок-API на json-server + авторизация (json-server-auth)",
  "private": true,
  "scripts": {
    "api": "json-server-auth db.json --port 3000",
    "serve": "npx serve -l 5173 .",
    "dev": "echo \"В одном терминале: npm run api, в другом: npm run serve\""
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0",
    "serve": "^14.2.1"
  }
}
